#include <stdio.h>
/**
 * main - prints arguenments count received
 * @argc: arguemnet count
 * @argv: array of pointers to argument strings
 * Return: 0
 **/
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
